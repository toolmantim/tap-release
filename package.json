{
  "name": "tap-release-bot",
  "version": "1.0.0",
  "description": "",
  "author": "Tim Lucas <t@toolmantim.com> (https://github.com/toolmantim)",
  "license": "ISC",
  "repository": "https://github.com/toolmantim/tap-release-bot.git",
  "scripts": {
    "dev": "nodemon --exec \"npm start\"",
    "start": "probot run ./index.js",
    "lint": "standard --fix",
    "test": "jest && standard",
    "test:watch": "jest --watch --notify --notifyMode=change --coverage",
    "simulate": "probot simulate release test/fixtures/release.json ./index.js"
  },
  "dependencies": {
    "compare-versions": "^3.2.1",
    "probot": "^6.0.0",
    "probot-config": "^0.1.0",
    "request": "^2.87.0"
  },
  "devDependencies": {
    "jest": "^22.4.3",
    "nock": "^9.2.6",
    "nodemon": "^1.17.2",
    "smee-client": "^1.0.1",
    "standard": "^10.0.3"
  },
  "engines": {
    "node": ">= 8.3.0"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "jest": {
    "collectCoverage": true,
    "collectCoverageFrom": ["index.js", "lib/**"]
  }
}
